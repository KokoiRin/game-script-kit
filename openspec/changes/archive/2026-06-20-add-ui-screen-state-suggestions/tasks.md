## 1. Application 和 HTTP

- [x] 1.1 增加状态配置名称读取 helper，并为去重、缺少配置和非法配置补测试。
- [x] 1.2 在 `LocalControlApplication` 暴露状态名列表用例。
- [x] 1.3 增加 HTTP endpoint 返回状态名候选。

## 2. 前端 UI

- [x] 2.1 给模拟状态输入增加 `datalist`。
- [x] 2.2 页面加载时请求状态名候选并填充 datalist。

## 3. 验证和归档

- [x] 3.1 运行相关测试、全量测试、`git diff --check` 和 OpenSpec 校验。
- [x] 3.2 合并主规格并归档 change。
