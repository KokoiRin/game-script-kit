# 项目结构分析（历史快照）

这份文档曾用于记录早期项目结构，但当前代码已经完成 `portable/` 和 `platform/` 分层重组，旧的 `domain/`、`engine/`、`adapters/` 顶层路径说明已经不再准确。

后续自动开发时不要把本文作为当前架构依据。当前长期约束以根目录 `agents.md` 为准，当前能力和使用方式以 `README.md` 与 `openspec/specs/` 为准。

保留本文的目的只是说明：项目曾经做过结构分析，并且后续应继续避免把核心规则、平台 adapter、入口协议和脚本定义混在同一个 module 中。
