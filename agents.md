# Agent Development Guide

本文件是后续自动开发时的长期约束。它只写稳定原则，不记录容易随实现演进变化的目录清单。修改代码前先读本文件；如果局部代码和本文件冲突，先按本文件判断，再用最小改动修正局部代码。

## 架构原则

项目采用 ports-and-adapters 思路：核心规则保持可测试、可移植，真实设备、平台权限、HTTP、CLI、UI 等副作用留在外层。

依赖方向必须从外到内：

```text
入口层 -> 平台装配层 -> 应用用例层 -> 执行/规则层 -> 领域模型层
```

基本原则：

- 内层不依赖外层。领域模型和执行规则不得导入平台、入口、UI、CLI 或真实设备实现。
- 外层可以依赖内层。入口层和 adapter 负责把外部世界转换成内层需要的 interface。
- 跨平台核心只表达规则和流程，不知道当前运行在 macOS、桌面 UI、终端还是测试环境。
- 真实设备、平台库、权限要求和网络/HTTP 细节只能出现在外层 adapter 或入口装配处。
- 新平台、新运行方式或新测试替身优先通过 adapter/composition 接入，不要把平台判断散落进核心逻辑。
- CLI/UI/HTTP handler 必须保持薄：解析输入、调用应用用例、展示结果；不得解释脚本语义或实现业务规则。

## Module 设计原则

每个 module 都应该有清晰 interface 和单一主职责。判断职责时按角色分层，而不是按当前目录名死记：

- 领域模型：表达概念、不变量和值对象；不执行副作用。
- 执行/规则：解释领域模型、调用 ports、处理纯规则；不创建真实 adapter。
- 应用用例：编排一次用户意图，归一化错误和结果；通过依赖注入接收外部能力。
- Adapter：在 seam 上实现 port，把外部系统接进来；隐藏平台库、权限和环境差异。
- 入口：处理 CLI、HTTP、UI 等协议细节；只做输入输出转换和 use case 调用。

设计 module 时做删除测试：

- 删除后复杂度会散落到多个调用方，说明这个 module 有深度。
- 删除后只是少一层转发，说明它太浅，不应新增或应该合并。

发现一个文件承担多种独立变化原因时，优先拆模块。暂时不拆时，必须用清晰分隔注释标出不同职责区域。

## Python 写法

每个 Python 文件开头必须有 module docstring，说明：

- 本文件负责什么。
- 本文件不负责什么。
- 它和相邻 module 的 interface/seam 关系。

每个函数或方法都必须有一行中文注释，优先使用一行 docstring：

```python
def run(self, script: Script) -> None:
    """按脚本步骤树顺序执行所有步骤。"""
```

复杂逻辑处必须补中文注释，说明“为什么这样做”，不要只复述代码。常见场景包括：

- 递归、轮询、超时、重试、资源释放。
- 错误归一化、安全白名单、禁止任意 shell。
- adapter 注入、延迟导入、测试替身、平台权限规避。

如果文件中暂时保留多种职责，用多行分隔注释隔开：

```python
# ---------------------------------------------------------------------------
# HTTP request adapter
# ---------------------------------------------------------------------------
```

## 测试原则

保持测试金字塔：

- 底层测试最多：领域模型、规则解释、纯 use case。使用 fake、dry-run、内存替身，验证公开行为。
- 中层测试适量：adapter contract、composition、CLI/HTTP 到 use case 的映射。
- 顶层测试最少：本地 UI、真实平台、真实设备只做必要 smoke，不作为主要覆盖来源。

测试应验证可观察行为，不绑定 private method、内部调用次数或临时实现结构。只有系统边界适合 mock，例如真实设备、文件系统、HTTP、时间、平台能力。

新增自动化能力时，至少考虑这些行为面：

- 领域模型和不变量。
- 执行语义。
- 运行能力需求和错误路径。
- dry-run 或 fake 环境下可重复验证的入口路径。
- 涉及 UI/CLI 时的用户可见结果。

## TDD 流程

新功能扩展使用行为驱动的 TDD：

```text
确认行为
  -> 写一条公开行为测试并确认失败
  -> 写最小实现让它通过
  -> 继续下一条行为
  -> 全部通过后重构
  -> 跑完整验证
```

原则：

- 一次只推进一个行为。
- 不要先横向写完一批测试再批量实现。
- GREEN 阶段只实现当前行为，不提前实现未来功能。
- 重构只在测试全绿后进行，重构后必须重新运行测试。
- 测试失败原因必须和当前行为相关；如果失败暴露了设计误判，先调整行为理解再继续。

## OpenSpec 原则

跨越多个 module、改变公开行为或影响架构方向的变化，先用 OpenSpec 明确行为和范围。小型文档修正、注释修正、单个脚本参数调整可以直接改。

需要 OpenSpec 的典型情况：

- 新增或修改 DSL/脚本语义。
- 修改执行规则或 port contract。
- 新增平台能力、adapter 类型或装配方式。
- 改变 CLI/UI 的公开行为。
- 调整核心 module 的依赖方向或职责划分。

完成后至少运行：

```bash
.venv/bin/python -m pytest
openspec validate --specs --strict
git diff --check
```

如果正在实现具体 OpenSpec change，还要运行对应 change 的 strict validate。

每完成一轮 OpenSpec change，包括实现、验证和归档后，必须立即创建一次 commit 并 push 到远端。commit message 使用中文，说明本轮改了什么，并在开头区分变更类型：

- 新增需求：用于新增能力、扩展公开行为、接入新 adapter 或新入口能力。
- Bugfix：用于修复缺陷、坐标偏差、错误路径、回归或环境兼容问题。

示例：

```bash
git commit -m "新增需求：接入 OpenCV 图片匹配"
git commit -m "Bugfix：修正 Retina 屏幕图片点击坐标"
```

## 停下来重审的信号

出现以下信号时，不要继续堆代码，先重新审视 module 设计：

- 入口层开始解释核心语义。
- 核心层开始知道平台、权限、UI、CLI 或真实设备。
- 同一规则散落在多个调用方。
- 为了测试不断暴露内部细节。
- 新增能力需要在很多无关文件同步修改。
- 一个文件的职责需要用很长文字才能解释清楚。

优先把复杂度收进更深的 module，让调用方学习更小的 interface，并把变化集中在一个地方。
